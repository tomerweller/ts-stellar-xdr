// Internal SDK utility — not part of the public compat API.
// Stub for parity tests; these tests are expected to fail.
export function best_r(rawNumber: string | number): { n: number; d: number } {
  throw new Error('best_r is an internal SDK utility not available in compat');
}
